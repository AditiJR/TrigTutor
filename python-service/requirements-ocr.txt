# Optional OCR extras — install with:
#   pip install -r requirements-ocr.txt
#
# Adds pix2tex (LaTeX-OCR) for local image → LaTeX extraction.  This is the
# fully open-source fallback for when MATHPIX_APP_ID is not set.
#
# pix2tex pulls in PyTorch and its weights (~80 MB on first run).  Reserve
# ~1 GB of disk space and ~2 GB of RAM for inference.

pix2tex>=0.1.4
Pillow>=10.0.0
